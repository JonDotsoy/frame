{
  "dependencies": {
    "@swc/core": "^1.3.21",
    "stacktrace-js": "^2.0.2",
    "ts-node": "^10.9.1"
  },
  "scripts": {
    "test": "node --test -r ts-node/register path-tools.test.ts",
    "prepare": "tsc"
  },
  "name": "tomy",
  "version": "0.1.0",
  "description": "TOMY is a framework for building scalable NodeJS applications based on the cleaner JS syntax, without decorators or sugar syntax.",
  "main": "dist/tomy.js",
  "bin": "dist/cli.js",
  "devDependencies": {
    "@types/node": "^18.11.11",
    "nodemon": "^2.0.20",
    "prettier": "^2.8.1"
  },
  "prepare": "tsc"
}
